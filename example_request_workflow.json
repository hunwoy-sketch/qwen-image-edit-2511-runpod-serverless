{
  "input": {
    "workflow": {
      "1": {
        "class_type": "CheckpointLoaderSimple",
        "inputs": { "ckpt_name": "Qwen-Rapid-AIO-NSFW-v23.safetensors" }
      },
      "7": {
        "class_type": "LoadImage",
        "inputs": { "image": "input.png" }
      },
      "_说明": "这里放完整的 ComfyUI API 格式工作流（在 ComfyUI 界面用 Workflow -> Export (API) 导出）。节点 ID、class_type、inputs 必须齐全。"
    },
    "images": [
      { "name": "input.png", "image_url": "https://picsum.photos/768" }
    ],
    "output_node": "6"
  }
}
